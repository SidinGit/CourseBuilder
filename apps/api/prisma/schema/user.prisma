model User {
    id        String   @id @default(cuid())
    email     String   @unique
    password  String
    name      String?
    courses   Course[]
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
